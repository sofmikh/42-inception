# Custom MariaDB configuration for Inception
[mysqld]
# Listen on all interfaces so the WordPress container can connect
# Without this, MariaDB would only accept connections from localhost
bind-address = 0.0.0.0
port         = 3306
# Data directory where databases are stored (mapped to a Docker volume)
datadir      = /var/lib/mysql
